// Import the new Compact component
import KaryScreenCompact from '../components/KaryScreenCompact';

// Export the Compact version directly
const KaryScreen = () => {
  return <KaryScreenCompact />;
};

export default KaryScreen;